import 'package:flutter/material.dart';

class SavedBooksView extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Your saved books'),
      ),
    );
  }
} 